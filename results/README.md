# Results

This directory stores generated experimental outputs.

Expected subdirectories:

- `raw/`: model JSONL predictions
- `tables/`: metric CSV files
- `figures/`: generated paper figures
- `logs/`: training and inference logs

No result in this directory should be edited manually.
